overwrite: true
schema: './src/graphql/schema.graphql'
documents: null
generates:
  src/graphql/schema.types.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
  ./graphql.schema.json:
    plugins:
      - 'introspection'
